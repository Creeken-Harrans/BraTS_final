# Models

`BraTS_final` 不再通过 plans 动态拼装网络。

当前固定模型直接定义在：

- `models/brats_unet_3d.py`

它表达的是项目唯一正式使用的 3D 分割网络：

- 4 输入通道
- 3 输出分割头
- 3D U-Net
- 固定 patch size `128 x 128 x 128`

这里保留的是已经在旧项目里实际训练过的那一套结构，但对外只保留当前项目自己的模型命名和固定配置。
